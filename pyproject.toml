[project]
name = "tech-career-platform-workspace"
version = "0.1.0"
license = { text = "MIT" }
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "api",
  "packages/core",
]

[dependency-groups]
dev = [
  "uvicorn>=0.30,<1",
]

[tool.pytest.ini_options]
addopts = [
  "--strict-markers",
]
markers = [
  "integration: marks tests that exercise cross-module or framework integration",
]

[tool.coverage.run]
branch = true
source = [
  "api",
  "core",
]
omit = [
  "*/test/*",
  "*/conftest.py",
  "*/scripts/*",
]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = false